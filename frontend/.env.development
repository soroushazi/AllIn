# VITE_API_URL is intentionally unset here - API calls go to the relative
# path /api/..., which the dev server proxies to the backend (vite.config.js)
# so the browser sees everything as one origin. Set this only if the backend
# is ever split onto a genuinely different domain.
